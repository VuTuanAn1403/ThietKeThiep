version: '3.8'

services:
  nha-co-tiec:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nha-co-tiec-web
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - NEXT_PUBLIC_SITE_URL=http://localhost:3000
